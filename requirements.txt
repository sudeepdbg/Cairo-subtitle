# ── Semantix · Video Intelligence Platform ────────────────────────────────
# requirements.txt  (updated for v5)
#
# Changes from previous version:
#   + pydantic>=2.0.0          required by instructor v1+ (model_dump(), Field max_length)
#   + groq>=0.9.0              Groq LLM client for AI metadata generation
#   + instructor>=1.3.0        structured outputs wrapper around Groq / OpenAI
#   + httpx>=0.27.0            HTTP client; pinned to avoid conflicts with groq/instructor
#   + typing-extensions>=4.9.0 needed by pydantic v2 on Python <3.11
#   ~ streamlit bumped 1.32→1.35  ensures st.query_params dict API, st.status(),
#                                  st.toggle(), st.container(border=)
# ──────────────────────────────────────────────────────────────────────────

# Web framework
streamlit>=1.35.0

# Data & numerics
numpy>=1.24.0
pandas>=2.0.0

# Visualisation
plotly>=5.18.0

# AI / LLM — metadata generation
pydantic>=2.0.0
groq>=0.9.0
instructor>=1.3.0

# HTTP (transitive dep of groq + instructor; pinned for stability)
httpx>=0.27.0

# Typing (needed by pydantic v2 on Python 3.9/3.10)
typing-extensions>=4.9.0

# Video intelligence — scene detection & search
scikit-learn>=1.3.0
rank-bm25>=0.2.2
sentence-transformers>=2.2.2
networkx>=3.1

# YouTube integration
google-api-python-client>=2.100.0
youtube-transcript-api>=0.6.1

# ──────────────────────────────────────────────────────────────────────────
# DEPLOYMENT NOTES
#
# Streamlit Cloud / resource-constrained environments:
#   sentence-transformers pulls PyTorch (~2 GB). To reduce slug size,
#   add these lines BEFORE sentence-transformers:
#
#       --extra-index-url https://download.pytorch.org/whl/cpu
#       torch>=2.0.0
#
# Python version: 3.10 or 3.11 recommended.
#   pydantic v2 + instructor v1 are not compatible with Python 3.8.
# ──────────────────────────────────────────────────────────────────────────
